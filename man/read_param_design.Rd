% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/param_design.R
\name{read_param_design}
\alias{read_param_design}
\title{Read archived outputs from a \code{\link[=run_param_design]{run_param_design()}} directory}
\usage{
read_param_design(
  keep_dir,
  output_files = NULL,
  output_reader = read_dlf,
  digits = NULL,
  reader_args = list(),
  sim_mutator = NULL
)
}
\arguments{
\item{keep_dir}{Directory created by \code{\link[=run_param_design]{run_param_design()}} (\code{keep_files = TRUE}).}

\item{output_files}{Named character vector of the \emph{original} Daisy output
paths (same as passed to \code{\link[=run_param_design]{run_param_design()}}). Used only to recover
the \verb{<filename>} suffix. If \code{NULL}, the \code{manifest.rds} written by
\code{\link[=run_param_design]{run_param_design()}} is used.}

\item{output_reader, digits, reader_args, sim_mutator}{Same meaning as in
\code{\link[=run_param_design]{run_param_design()}}. \code{sim_mutator} is applied after reading, before
runs are stacked; use it to subset columns (there is no \code{cols} argument).}
}
\value{
A list with \code{design} (from \code{design.csv}) and \code{outputs}. \code{outputs}
is a \code{data.table} of all runs when a single file was kept, with a
\code{run_id} column; if several \code{output_files} were kept, a named list of
such data.tables (one per file).
}
\description{
Independent of \code{\link[=run_param_design]{run_param_design()}}: given a \code{keep_dir} produced with
\code{keep_files = TRUE}, reads every \verb{run_<id>_<filename>} copy and row-binds
them into one table per output file. Parameter values are not copied onto
those rows - join \code{design} on \code{run_id} later if you need them.
}
\examples{
\dontrun{
# After run_param_design(..., output_files = c(harvest = ..., swc = ...),
# keep_dir = "metamodel_runs"): two stacked tables, not one mixed table.
got <- read_param_design(
  "metamodel_runs",
  sim_mutator = list(
    harvest = function(dt) {
      dt <- add_date(dt, "harvest")
      dt[, c("Date", "WSOrg"), with = FALSE]
    },
    swc = function(dt) {
      dt <- add_date(dt, "swc")
      dt[, c("Date", "Theta @ -1.25"), with = FALSE]
    }
  )
)

got$design              # all parameter rows
got$outputs$harvest     # all runs, harvest columns + run_id
got$outputs$swc         # all runs, swc columns + run_id

# Parameter values are not copied onto the output rows; join when needed:
harvest <- merge(got$design, got$outputs$harvest, by = "run_id")
swc     <- merge(got$design, got$outputs$swc,     by = "run_id")
}

}
\seealso{
\code{\link[=run_param_design]{run_param_design()}}
}
