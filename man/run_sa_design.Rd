% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensitivity.R
\name{run_sa_design}
\alias{run_sa_design}
\title{Run Daisy for every row of a sensitivity design matrix}
\usage{
run_sa_design(
  sa_obj,
  config,
  run_file,
  daisy_exe = NULL,
  output_files,
  response_fun,
  working_dir = NULL,
  template_dir = ".",
  output_dir = ".",
  show_log = FALSE,
  output_reader = read_dlf,
  progress = TRUE,
  cmd = NULL
)
}
\arguments{
\item{sa_obj}{Object returned by \code{\link[=sa_design]{sa_design()}}.}

\item{config}{A \code{daisyr_param_config} object (normally
\code{attr(sa_obj, "config")}, i.e. the same one used to build \code{sa_obj}).}

\item{run_file, daisy_exe, working_dir, show_log, cmd}{Passed to \code{\link[=run_daisy]{run_daisy()}}.
\code{daisy_exe} may be \code{NULL} to use \code{\link[=get_daisy_path]{get_daisy_path()}}.}

\item{output_files}{Named character vector/list of Daisy output file
paths (e.g. \code{c(swc = "Output/interval_water_content.dlf")}) to read
after each run and pass to \code{response_fun}.}

\item{response_fun}{Function taking a named list of tables (one per
\code{output_files} entry, read via \code{output_reader}) and returning a numeric
scalar (or a fixed-length numeric vector, for multivariate Sobol
analyses) summarising that run for the sensitivity analysis.}

\item{template_dir, output_dir}{Passed to \code{\link[=render_templates]{render_templates()}}.}

\item{output_reader}{Function used to read each entry of \code{output_files}.
Defaults to \code{\link[=read_dlf]{read_dlf()}}.}

\item{progress}{If \code{TRUE} (the default), print a console progress bar
(\code{\link[utils:txtProgressBar]{utils::txtProgressBar()}}) over the Daisy runs. Set \code{FALSE} to keep
stdout clean. A function \verb{function(i, n)} is called after every run
(\code{i} completed out of \code{n}), and once with \code{i = 0} before the first run
— use this from Shiny via \code{\link[shiny:withProgress]{shiny::setProgress()}}.}
}
\value{
A list with \code{run_log} (a \code{data.table} recording the parameter
values used for every run) and \code{responses} (a matrix, one row per run).
}
\description{
For each parameter set in \code{sa_obj$X}, renders the config's templates,
runs Daisy, reads the requested output file(s), and calls \code{response_fun}
on them to produce the scalar (or vector) response that
\code{\link[=complete_sa_analysis]{complete_sa_analysis()}} will later attach to the design via \code{tell()}.
}
