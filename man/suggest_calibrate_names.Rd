% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensitivity.R
\name{suggest_calibrate_names}
\alias{suggest_calibrate_names}
\title{Suggest calibration names from a sensitivity index table}
\usage{
suggest_calibrate_names(indices, config, frac = 0.1)
}
\arguments{
\item{indices}{A data.frame from \code{\link[=complete_sa_analysis]{complete_sa_analysis()}} / \code{plot_sa()},
with a \code{name} column and either \code{mu_star} or \code{ST}.}

\item{config}{A \code{daisyr_param_config} used to expand PLF groups.}

\item{frac}{Relative threshold (default \code{0.1}).}
}
\value{
Character vector of parameter names, in config order.
}
\description{
Keeps names whose Morris \eqn{\mu^*} (or Sobol \code{ST}) is at least \code{frac}
of the maximum, then expands PLF curves to whole units via
\code{\link[=expand_calibrate_names]{expand_calibrate_names()}}. If nothing passes the cut, the top-ranked
name (plus its PLF unit) is kept.
}
