% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_dwf.R
\name{write_dwf}
\alias{write_dwf}
\title{Write a Daisy weather file (\code{.dwf}).}
\usage{
write_dwf(
  data,
  path,
  station,
  elevation,
  latitude,
  longitude,
  timezone = NULL,
  surface = "reference",
  screen_height = 2,
  timestep = "24 hours",
  begin = NULL,
  end = NULL,
  notes = NULL,
  nh4_wet_dep = 0.9,
  nh4_dry_dep = 2.2,
  no3_wet_dep = 0.6,
  no3_dry_dep = 1.1,
  t_average = NULL,
  t_amplitude = NULL,
  max_t_day = NULL,
  units = NULL,
  extra = NULL,
  comment = "Daisy weather file."
)
}
\arguments{
\item{data}{A \code{data.frame} or \link[data.table:data.table]{data.table::data.table} of weather
observations.}

\item{path}{Character scalar. File to write (usually \verb{*.dwf}).}

\item{station}{Character scalar. Station name written as \verb{Station:}.}

\item{elevation}{Numeric. Station elevation in metres.}

\item{latitude, longitude}{Numeric. Latitude (\code{dgNorth}) and longitude
(\code{dgEast}). Western longitudes are negative.}

\item{timezone}{Numeric or \code{NULL}. Time zone in \code{dgEast}. If \code{NULL},
rounded to the nearest 15 degrees of \code{longitude}.}

\item{surface}{Character scalar. Measurement surface (Daisy default
\code{"reference"}).}

\item{screen_height}{Numeric. Screen height in metres.}

\item{timestep}{Character scalar passed to Daisy (e.g. \code{"24 hours"}).}

\item{begin, end}{\code{Date} or \code{NULL}. Period covered; default is the min/max
date in \code{data}.}

\item{notes}{Character vector of \verb{Note:} lines, or \code{NULL}.}

\item{nh4_wet_dep, nh4_dry_dep, no3_wet_dep, no3_dry_dep}{Nitrogen
deposition (same defaults as the original helper / Taastrup sample).}

\item{t_average, t_amplitude, max_t_day}{Climate header values. \code{NULL}
(default) computes them from an \code{AirTemp} column.}

\item{units}{Optional named character vector of dimension strings,
keyed by the \strong{output} column names (\code{Year}, \code{GlobRad}, ...).}

\item{extra}{Optional named character vector of additional \code{Key: value}
header lines (e.g. \code{c(PrecipCorrect = "1.1 1.1 ...")}).}

\item{comment}{Character scalar placed on the first line after
\verb{dwf-0.0 -- }.}
}
\value{
\code{path}, invisibly.
}
\description{
Adapted from the project helper \code{writeDaisyWeatherFile()}: the table of
daily (or hourly) values is wrapped in Daisy's \code{dwf-0.0} header. Station
name and coordinates are arguments instead of being hardcoded to
Taastrup. Begin/End dates come from the table. Climate statistics
(\code{TAverage}, \code{TAmplitude}, \code{MaxTDay}) default to values computed from
\code{AirTemp} when that column is present.
}
\details{
Date columns may be \code{Year}/\code{Month}/\code{Day} (any case), the older
\code{YEAR}/\code{MM}/\code{DD} names, or a single \code{Date} column. Remaining columns are
written in file order. Units come from \code{units}, then \code{attr(data, "units")}
(as set by \code{\link[=read_dlf]{read_dlf()}} / \code{\link[=set_unit]{set_unit()}}), then built-in Daisy dimensions
for common names (\code{GlobRad}, \code{AirTemp}, \code{Precip}, ...).
}
\examples{
\dontrun{
wx <- data.frame(
  Date = as.Date("2000-01-01") + 0:2,
  GlobRad = c(20, 40, 35),
  AirTemp = c(2.1, 3.0, 1.5),
  Precip = c(0, 1.2, 0)
)
write_dwf(wx, "site.dwf", station = "Example",
          elevation = 30, latitude = 56, longitude = 12)
}
}
\seealso{
\code{\link[=run_daisy]{run_daisy()}}, \code{\link[=set_unit]{set_unit()}}
}
