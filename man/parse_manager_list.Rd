% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dai_programs.R
\name{parse_manager_list}
\alias{parse_manager_list}
\title{Inverse of \code{render_manager_list()}. Reconstructs a flat list of names
(never the \verb{\{use:, repeat:\}} shorthand -- Daisy's own text has no way
to tell "written out 20 times" apart from "20 different lines that
happen to repeat", and a flat list of the same name N times regenerates
the exact same \code{.dai} text either way).}
\usage{
parse_manager_list(children)
}
\description{
Inverse of \code{render_manager_list()}. Reconstructs a flat list of names
(never the \verb{\{use:, repeat:\}} shorthand -- Daisy's own text has no way
to tell "written out 20 times" apart from "20 different lines that
happen to repeat", and a flat list of the same name N times regenerates
the exact same \code{.dai} text either way).
}
\keyword{internal}
