% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dai_parse_helpers.R
\name{.dai_parse_wait_form}
\alias{.dai_parse_wait_form}
\title{Inverse of \code{render_wait_from_date()}: given a node that might be a
\verb{(wait_mm_dd ..)} / \verb{(wait_days ..)} / \verb{(wait COND)} form, reconstruct
the \code{date} spec it came from. Returns \code{NULL} if \code{node} isn't one of
these three shapes at all (the caller then knows this isn't a wait).}
\usage{
.dai_parse_wait_form(node)
}
\description{
Inverse of \code{render_wait_from_date()}: given a node that might be a
\verb{(wait_mm_dd ..)} / \verb{(wait_days ..)} / \verb{(wait COND)} form, reconstruct
the \code{date} spec it came from. Returns \code{NULL} if \code{node} isn't one of
these three shapes at all (the caller then knows this isn't a wait).
}
\keyword{internal}
