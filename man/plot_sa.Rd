% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensitivity.R
\name{plot_sa}
\alias{plot_sa}
\title{Plot sensitivity-analysis results}
\usage{
plot_sa(sa_obj, type = NULL, main = NULL, ...)
}
\arguments{
\item{sa_obj}{A \code{morris} or Sobol object (\code{sobol} / \code{soboljansen})
returned by \code{\link[=complete_sa_analysis]{complete_sa_analysis()}} (i.e. after \code{\link[sensitivity:decoupling]{sensitivity::tell()}}
has attached the responses).}

\item{type}{Plot variant. For Morris, \code{"effects"} (default) or \code{"bar"}.
For Sobol, \code{"bar"} (default) or \code{"ranked"}.}

\item{main}{Plot title. Sensible defaults depend on \code{type} / method.}

\item{...}{Unused; kept for compatibility with earlier base-graphics calls.}
}
\value{
Invisibly, a \code{data.frame} of the plotted indices: \code{name},
\code{mu}, \code{mu_star}, \code{sigma} for Morris; \code{name}, \code{S}, \code{ST} (and CI)
columns when present) for Sobol.
}
\description{
Visualises the indices computed by \code{\link[=complete_sa_analysis]{complete_sa_analysis()}} with ggplot2
(same stack as the Shiny Plots page). Morris \eqn{\mu^*} vs \eqn{\sigma}
labels use \code{\link[ggrepel:geom_text_repel]{ggrepel::geom_text_repel()}}.
}
\details{
\describe{
\item{Morris}{Default \code{type = "effects"} is the Campolongo plot of
\eqn{\mu^*} (mean absolute elementary effect; importance) against
\eqn{\sigma} (standard deviation of elementary effects;
non-linearity / interactions), with one labelled point per parameter.
\code{type = "bar"} is a horizontal bar chart of \eqn{\mu^*} ranked
smallest to largest.}
\item{Sobol}{Default \code{type = "bar"} is a grouped bar chart of
first-order (\code{S}) and total (\code{ST}) indices per parameter.
Confidence-interval whiskers are added when the underlying
\code{sensitivity} object provides \verb{min. c.i.} / \verb{max. c.i.} columns.
\code{type = "ranked"} is a horizontal bar chart of total indices.}
}
}
\examples{
yaml <- tempfile(fileext = ".yaml")
writeLines("
parameters:
  - {name: p1, default: 0.5, min: 0, max: 1, from_file: a.dai, to_file: b.dai}
  - {name: p2, default: 0.5, min: 0, max: 1, from_file: a.dai, to_file: b.dai}
", yaml)
config <- read_param_config(yaml)
design <- sa_design(config, method = "morris", r = 5, seed = 1)
# Stand-in for Daisy responses: a simple function of the design matrix.
y <- as.matrix(design$X) \%*\% c(2, 0.1)
sa <- complete_sa_analysis(design, y)
plot_sa(sa)

}
\seealso{
\code{\link[=complete_sa_analysis]{complete_sa_analysis()}}, \code{\link[=sa_design]{sa_design()}}
}
