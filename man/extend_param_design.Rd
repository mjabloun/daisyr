% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/param_design.R
\name{extend_param_design}
\alias{extend_param_design}
\title{Add points to an existing Sobol' parameter design}
\usage{
extend_param_design(
  existing_design,
  params = NULL,
  n_additional,
  method = "sobol"
)
}
\arguments{
\item{existing_design}{A \code{daisyr_param_design} from
\code{\link[=generate_param_design]{generate_param_design()}} or a previous \code{\link[=extend_param_design]{extend_param_design()}} call.
Always pass the most recently returned object if you are adding points
in several batches (its \code{n_drawn} attribute is the sequence offset).}

\item{params}{Parameter bounds. Defaults to \code{attr(existing_design, "params")}.}

\item{n_additional}{Number of new rows to draw.}

\item{method}{Must be \code{"sobol"}.}
}
\value{
A \code{daisyr_param_design} of \code{n_additional} rows (new points only).
}
\description{
Continues the same Sobol' sequence from where \code{existing_design} left off
(indices \code{n_drawn + 1} through \code{n_drawn + n_additional}), so the old and
new points together remain a single low-discrepancy design. Returns only
the new rows, with the same columns as \code{\link[=generate_param_design]{generate_param_design()}}.
}
\details{
Optimized LHS designs are not extensible: calling this with
\code{method = "lhs"} or \code{method = "dice_lhs"} (or an existing_design built
with either) stops and tells you to call \code{\link[=generate_param_design]{generate_param_design()}} with
a larger \code{n} instead.
}
\seealso{
\code{\link[=generate_param_design]{generate_param_design()}}
}
