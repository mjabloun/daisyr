% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metamodel.R
\name{validate_metamodel}
\alias{validate_metamodel}
\title{Validate a fitted metamodel before trusting it}
\usage{
validate_metamodel(
  metamodel,
  scored_design,
  method = c("loo", "kfold"),
  K = 10,
  ...
)
}
\arguments{
\item{metamodel}{A \code{daisyr_metamodel} from \code{\link[=fit_metamodel]{fit_metamodel()}}.}

\item{scored_design}{The same \code{scored_design} \code{fit_metamodel()} was
fit on (needed to recover the true response values for \code{method = "loo"}'s Q2/RMSE, and to rebuild \code{X}/\code{Y} for \code{method = "kfold"}).}

\item{method}{\code{"loo"} (default) or \code{"kfold"}.}

\item{K}{Number of folds for \code{method = "kfold"} (default 10); ignored
for \code{"loo"}.}

\item{...}{For \code{"loo"}, forwarded to \code{DiceKriging::leaveOneOut.km()}
(e.g. \code{trend.reestim}). For \code{"kfold"}, forwarded to
\code{DiceEval::modelFit()} (e.g. \code{formula}).}
}
\value{
A \code{daisyr_metamodel_validation}: a list with \code{method} and
\code{result}. For \code{"loo"}, \code{result} has \code{predicted}, \code{sd}, \code{residuals},
\code{Q2}, and \code{RMSE}. For \code{"kfold"}, \code{result} is whatever
\code{DiceEval::crossValidation()} returns (\code{Q2}, \code{RMSE_CV}, \code{MAE_CV},
\code{Ypred}, \code{folds}, ...).
}
\description{
A Kriging surrogate is only as useful as its predictive accuracy on
points it wasn't fit to -- this checks that before the metamodel is used
for sensitivity analysis, uncertainty bounds, or \code{\link[=calibrate_daisy_ego]{calibrate_daisy_ego()}}.
}
\details{
\describe{
\item{\code{method = "loo"}}{Leave-one-out cross-validation via
\code{DiceKriging::leaveOneOut.km()} -- refits nothing extra (Kriging's
LOO has a closed form), returns predicted values/sd for every
training point with itself removed, and a summary Q2 (predictivity
coefficient; 1 is a perfect fit, 0 means no better than predicting
the mean) and RMSE.}
\item{\code{method = "kfold"}}{K-fold cross-validation via
\code{DiceEval::modelFit()} + \code{DiceEval::crossValidation()}. \code{DiceEval}
refits its own model internally (once up front via \code{modelFit()},
then once per fold inside \code{crossValidation()}) rather than reusing
\code{metamodel$km} directly -- this rebuilds it as a \code{type = "Kriging"}
\code{DiceEval} model with the same \code{covtype} as \code{metamodel}, so the two
stay comparable. Useful mainly because \code{DiceEval::modelFit()} can
also build \code{"Linear"}/\code{"MARS"}/etc. metamodels on the same design if
you want to check whether Kriging is actually buying you anything
over something cheaper. Requires the \code{DiceEval} package.}
}
}
\examples{
\dontrun{
check <- validate_metamodel(metamodel, scored)
check$result$Q2
check$result$RMSE
}
}
\seealso{
\code{\link[=fit_metamodel]{fit_metamodel()}}
}
