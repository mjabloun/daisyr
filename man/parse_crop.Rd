% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dai_crops.R
\name{parse_crop}
\alias{parse_crop}
\title{Parse a \verb{(defcrop name parent [description] field...)} form's body
children (i.e. everything after the leading \code{defcrop} keyword atom)
back into one \code{crops[[i]]} entry -- the inverse of \code{render_crop()},
also accepting a bare (unquoted) name and/or Daisy's optional
positional description string (see dai_parse_helpers.R). The block's
own \code{comment} field, if any, is attached by the caller (\code{parse_dai()},
dai_read.R), not here.}
\usage{
parse_crop(body_children)
}
\description{
Parse a \verb{(defcrop name parent [description] field...)} form's body
children (i.e. everything after the leading \code{defcrop} keyword atom)
back into one \code{crops[[i]]} entry -- the inverse of \code{render_crop()},
also accepting a bare (unquoted) name and/or Daisy's optional
positional description string (see dai_parse_helpers.R). The block's
own \code{comment} field, if any, is attached by the caller (\code{parse_dai()},
dai_read.R), not here.
}
\keyword{internal}
