% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dai_read.R
\name{read_dai}
\alias{read_dai}
\title{Read a Daisy \code{.dai} file into the config list \code{generate_dai()} consumes}
\usage{
read_dai(dai_path, yaml_path = NULL)
}
\arguments{
\item{dai_path}{Character scalar. Path to the input \code{.dai} file.}

\item{yaml_path}{Character scalar, or \code{NULL} (default). If given, the
parsed config is also written to this path as YAML (via
\code{yaml::write_yaml()}).}
}
\value{
The parsed config list (see \code{parse_dai()}).
}
\description{
Reads \code{dai_path}, parses it with \code{parse_dai()}, and returns the config
list -- so a file can be read, edited in R, and regenerated:
\code{generate_dai(read_dai("west.dai"))} reproduces it. Pass \code{yaml_path} to
also write the parsed config out as a YAML file, e.g. to hand-edit from
there instead, or to check what \code{read_dai()} recovered.
}
\examples{
\dontrun{
config <- read_dai("west.dai")
config$programs[[1]]$log_prefix <- "revised_"
generate_dai(config, output_path = "west_revised.dai")

## Or just convert an existing .dai file to YAML, to edit by hand:
read_dai("west.dai", yaml_path = "west.yaml")
}
}
