% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/template.R
\name{render_templates}
\alias{render_templates}
\title{Render every \code{.dai} template implied by the parameters file}
\usage{
render_templates(config, values, template_dir = ".", output_dir = ".")
}
\arguments{
\item{config}{A \code{daisyr_param_config} object, as returned by
\code{\link[=read_param_config]{read_param_config()}}.}

\item{values}{Named numeric vector (or list) with one entry per name in
\code{\link[=param_config_names]{param_config_names()}} - i.e. every direct parameter and every
\code{plf_curves} shape parameter.}

\item{template_dir}{Character scalar. Directory \code{from_file} paths are
relative to. Defaults to the current directory.}

\item{output_dir}{Character scalar. Directory \code{to_file} paths are written
relative to. Defaults to the current directory.}
}
\value{
Invisibly, a character vector of the \code{to_file} paths written.
}
\description{
For every \verb{(from_file, to_file)} pair referenced in \code{config}, reads the
template, substitutes every declared placeholder (direct parameters
verbatim, \code{plf_curves} blocks computed from their shape parameters), and
writes the result. This is the calibration/SA equivalent of the original
\code{updateParameters()}/\code{f.update()} pair, generalised to support multiple
template/target file pairs and PLF curve generation in one call.
}
