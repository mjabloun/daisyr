% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/param_config_scaffold.R
\name{create_param_config}
\alias{create_param_config}
\title{Create a starter parameters YAML file}
\usage{
create_param_config(
  parameters,
  type = "scalar",
  curve = "logistic",
  path = NULL
)
}
\arguments{
\item{parameters}{Character vector of parameter (or PLF) names.}

\item{type}{Character scalar or vector, recycled to \code{length(parameters)}.
Either \code{"scalar"} (a plain calibration/SA parameter, substituted
directly into a \code{{{name}}} placeholder) or \code{"plf"} (a piecewise-linear
function generated from a curve family's shape parameters, scaffolding
both the shape parameters and the \code{plf_curves} entry that consumes
them - see \code{curve}).}

\item{curve}{Character scalar or vector, recycled to \code{length(parameters)}.
One of \code{\link[=known_plf_curves]{known_plf_curves()}}. Only used for entries with \code{type = "plf"};
determines which shape parameters get scaffolded (e.g. \code{L}/\code{k}/\code{x0} for
\code{"logistic"}).}

\item{path}{Character scalar, optional. If given, writes the YAML text to
this file and returns the path invisibly. Otherwise returns the YAML
text as a character scalar (e.g. to \code{cat()} or edit further before
writing).}
}
\value{
Either \code{path} (invisibly, if given) or the assembled YAML text.
}
\description{
Scaffolds a YAML parameters file with one stub entry per name in
\code{parameters}, so you don't have to write the config schema from
scratch by hand - fill in the \code{REPLACE_ME.dai}/bounds placeholders it
leaves behind (marked \verb{# TODO}), then load it with
\code{\link[=read_param_config]{read_param_config()}} and check it with \code{\link[=validate_param_config]{validate_param_config()}}. See
\code{vignette("daisyr")} for the full schema.
}
\examples{
cat(create_param_config(c("Ap_clay", "LAIvsDS"), type = c("scalar", "plf")))
}
