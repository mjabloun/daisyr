% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plf_io.R
\name{plot_plf}
\alias{plot_plf}
\title{Plot a piecewise linear function}
\usage{
plot_plf(
  x,
  parameter = NULL,
  occurrence = 1,
  xlab = "x",
  ylab = "y",
  main = parameter,
  ...
)
}
\arguments{
\item{x}{Either a character scalar giving the path to a \code{.dai} file (in
which case \code{parameter} is required), or a \code{data.frame}/list with \code{x}
and \code{y} elements, such as the return value of \code{\link[=read_plf]{read_plf()}}.}

\item{parameter}{Character scalar. Name of the PLF parameter to extract;
required when \code{x} is a file path, ignored otherwise.}

\item{occurrence}{Integer. Passed to \code{\link[=read_plf]{read_plf()}} when \code{x} is a file path.}

\item{xlab, ylab, main}{Plot labels. \code{main} defaults to \code{parameter} when
\code{x} is a file path, or \code{NULL} otherwise.}

\item{...}{Passed on to \code{\link[graphics:plot.default]{graphics::plot()}}.}
}
\value{
Invisibly, a \code{data.frame} with columns \code{x} and \code{y}.
}
\description{
Plots a PLF's \verb{(x y)} points connected by straight line segments
(matching how Daisy itself interpolates a PLF between knots). Accepts
either a \code{.dai} file and parameter name (in which case it calls
\code{\link[=read_plf]{read_plf()}} itself) or an already-extracted \code{data.frame} - e.g. so you
can inspect/subset the result of \code{\link[=read_plf]{read_plf()}} before plotting it.
}
\examples{
\dontrun{
plot_plf("dk-sbarley.dai", "TempEff1")

df <- read_plf("dk-sbarley.dai", "TempEff1")
plot_plf(df, main = "TempEff1")
}
}
