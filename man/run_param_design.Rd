% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/param_design.R
\name{run_param_design}
\alias{run_param_design}
\title{Run Daisy for every row of a metamodel parameter design}
\usage{
run_param_design(
  design,
  config,
  run_file,
  daisy_exe = NULL,
  output_files,
  working_dir = NULL,
  template_dir = ".",
  output_dir = ".",
  show_log = FALSE,
  keep_files = TRUE,
  keep_dir = "param_design_runs",
  read = NULL,
  output_reader = read_dlf,
  digits = NULL,
  reader_args = list(),
  sim_mutator = NULL,
  progress = TRUE,
  cmd = NULL
)
}
\arguments{
\item{design}{A \code{data.frame} from \code{\link[=generate_param_design]{generate_param_design()}} (or any
table whose columns are parameter names).}

\item{config}{A \code{daisyr_param_config} used to render \code{.dai} templates.}

\item{run_file, daisy_exe, working_dir, show_log, cmd}{Passed to \code{\link[=run_daisy]{run_daisy()}}.
\code{daisy_exe} may be \code{NULL} to use \code{\link[=get_daisy_path]{get_daisy_path()}}.}

\item{output_files}{Named character vector of Daisy output paths relative
to \code{working_dir} (e.g. \code{c(harvest = "Output/harvest.dlf", swc = "Output/interval_water_content.dlf")}). Only these files are archived
or read; other Daisy outputs are ignored.}

\item{template_dir, output_dir}{Passed to \code{\link[=render_templates]{render_templates()}}.}

\item{keep_files}{If \code{TRUE} (default), copy each selected file into
\code{keep_dir} as \verb{run_<id>_<filename>} after each run.}

\item{keep_dir}{Directory for the renamed copies and \code{design.csv}.
Ignored when \code{keep_files = FALSE}.}

\item{read}{If \code{TRUE}, read \code{output_files} after each run (via
\code{output_reader}) and return them. Defaults to \code{FALSE} when
\code{keep_files = TRUE} (read later with \code{\link[=read_param_design]{read_param_design()}}), and to
\code{TRUE} when \code{keep_files = FALSE}. Setting both to \code{FALSE} is an error.}

\item{output_reader}{Function used to parse each file. First argument is
the path. Defaults to \code{\link[=read_dlf]{read_dlf()}}.}

\item{digits}{Forwarded to \code{output_reader} when non-\code{NULL} (matches
\code{\link[=read_dlf]{read_dlf()}}'s rounding argument).}

\item{reader_args}{Named list of additional arguments forwarded to
\code{output_reader} (merged with \code{digits}; \code{reader_args} wins on conflict).}

\item{sim_mutator}{Optional function applied to each simulated table after
reading - the same role as in \code{\link[=compute_generic_objective]{compute_generic_objective()}}. Use it to
subset columns, \code{\link[=add_date]{add_date()}}, rename fields, or derive metrics. A named
list of functions, keyed by \code{output_files} names, applies a different
mutator per file; missing names are left unchanged.}

\item{progress}{If \code{TRUE} (the default), print a console progress bar.
\code{FALSE} disables it. A function \verb{function(i, n)} is called after every
Daisy run (\code{i} completed out of \code{n}), and once with \code{i = 0} before the
first run.}
}
\value{
A list with:
\itemize{
\item \code{design} - the input design with a \code{run_id} column prepended.
\item \code{keep_dir} - the archive directory, or \code{NULL} if \code{keep_files = FALSE}.
\item \code{outputs} - if \code{read = TRUE}, the stacked tables (same shape as
\code{\link[=read_param_design]{read_param_design()}}); otherwise \code{NULL}.
}
}
\description{
For each row of \code{design}, renders the config's templates, runs Daisy,
then optionally archives selected output files and/or reads them.
}
\details{
Daisy overwrites its output directory in place, so without archiving or
reading, a later row would destroy earlier results. Kept files are copied
into a \strong{single} \code{keep_dir} (no per-row folders - that does not scale to
thousands of combinations) and renamed \verb{run_<id>_<filename>}, e.g.
\code{run_0001_harvest.dlf}. \code{design.csv} in the same directory maps each
\code{run_id} to its parameter combination.

Daisy can write many log files; only paths listed in \code{output_files} are
kept (and/or read). Reading can happen here or later via
\code{\link[=read_param_design]{read_param_design()}}. If you choose not to keep files (\code{keep_files = FALSE}),
reading is mandatory - otherwise the outputs would be lost when the next
row runs. A custom \code{output_reader} is accepted (default \code{\link[=read_dlf]{read_dlf()}}).
Restrict or reshape columns with \code{sim_mutator} (same idea as
\code{\link[=compute_generic_objective]{compute_generic_objective()}}), not a separate \code{cols} argument.
}
\examples{
\dontrun{
output_files <- c(
  harvest = "Output/harvest.dlf",
  swc     = "Output/interval_water_content.dlf"
)

# Run Daisy for every design row; keep both logs, do not read yet
# (typical for thousands of combinations). Files land in keep_dir as
# run_0001_harvest.dlf, run_0001_interval_water_content.dlf, ...
run <- run_param_design(
  design, config, run_file, daisy_exe,
  output_files = output_files,
  working_dir  = "path/to/daisy/scenario",
  keep_files   = TRUE,
  keep_dir     = "metamodel_runs"
)
run$outputs   # NULL: nothing was read in-loop
run$design    # run_id + parameter values (also written to design.csv)

# Same two files, read in-loop (small n). outputs is a named list of
# two stacked data.tables, one per output_files name.
run_and_read <- run_param_design(
  design, config, run_file, daisy_exe,
  output_files = output_files,
  working_dir  = "path/to/daisy/scenario",
  keep_files   = TRUE,
  keep_dir     = "metamodel_runs",
  read         = TRUE,
  sim_mutator  = list(
    harvest = function(dt) {
      dt <- add_date(dt, "harvest")
      dt[, c("Date", "WSOrg"), with = FALSE]
    },
    swc = function(dt) {
      dt <- add_date(dt, "swc")
      dt[, c("Date", "Theta @ -1.25"), with = FALSE]
    }
  )
)
run_and_read$outputs$harvest
run_and_read$outputs$swc
}

}
\seealso{
\code{\link[=generate_param_design]{generate_param_design()}}, \code{\link[=read_param_design]{read_param_design()}}, \code{\link[=run_sa_design]{run_sa_design()}}
}
