% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dai_generate.R
\name{generate_dai}
\alias{generate_dai}
\title{Generate Daisy \code{.dai} text from a config list}
\usage{
generate_dai(config, output_path = NULL)
}
\arguments{
\item{config}{A list in the schema documented at the top of this file
(top-level keys: \code{directory}, \code{path}, \code{libraries}, \code{comment},
\code{horizons}, \code{columns}, \code{crops}, \code{activities}, \code{programs}, \code{run},
\code{daisy_script}) --
the same shape \code{yaml::read_yaml()} produces from a YAML file, built
however you like. Every key is optional.}

\item{output_path}{Character scalar, or \code{NULL} (default). If given, the
generated text is also written to this path (via \code{writeLines()}).}
}
\value{
The generated \code{.dai} text, as a character scalar.
}
\description{
The engine behind \code{yaml_to_dai()}, and a supported entry point in its
own right -- not just an internal step. \code{yaml_to_dai()} does exactly
one thing before handing off to this function: read a YAML file into
the nested-list shape \code{generate_dai()} expects (a YAML mapping becomes
a named list, a sequence becomes an unnamed list -- the schema
documented at the top of this file, and per-section in
dai_management.R / dai_horizons.R / dai_columns.R / dai_crops.R /
dai_programs.R). Nothing about \code{generate_dai()} itself cares where
that list came from.
}
\details{
That matters most once you need many \code{.dai} files, not one -- e.g.
running Daisy across many sites in a country, not just one field.
Build \code{config} directly in an R script instead of writing a YAML file
by hand: as a plain nested list, assembled programmatically from a
table of site/soil/crop data you already have (see \code{records_from_df()}
in dai_dataframe.R, which turns one data.frame row into one record --
e.g. one \code{defhorizon} or one column's \code{Soil.horizons} reference), or
some mix of both. A loop over the rows of a sites table, each building
its own \code{config} and calling \code{generate_dai(config, output_path = ...)},
is the natural way to automate a batch of \code{.dai} files this way.
}
\examples{
\dontrun{
## Built entirely as a plain R list, no YAML file involved:
config <- list(
  crops = list(list(name = "Scot Barley", based_on = "Spring Barley",
                     Devel = list("original", list(DSRate2 = 0.025))))
)
cat(generate_dai(config))

## One .dai file per row of a sites table:
for (i in seq_len(nrow(sites))) {
  site <- sites[i, ]
  site_config <- list(
    columns = list(list(name = site$column, Soil = list(...), Movement = list(...))),
    programs = list(list(name = site$name, type = "Daisy",
                          weather = list(file = site$weather_file),
                          column = site$column, ...)),
    run = list(site$name)
  )
  generate_dai(site_config, output_path = paste0(site$name, ".dai"))
}
}
}
