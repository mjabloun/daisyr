% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dai_helpers.R
\name{.render_parent_ref}
\alias{.render_parent_ref}
\title{Render the parent/inheritance positional argument shared by \code{defcolumn},
\code{defcrop}, and (for the inheriting-program case) \code{defprogram}: the bare
keyword \code{default} when no real parent is named (Daisy's own "use the
library default" convention, e.g. \verb{(defcolumn "X" default ...)} /
\verb{(defcrop "Spring Barley" default ...)}), or a quoted reference to
another named object otherwise (\verb{(defprogram "west" "Common" ...)}).}
\usage{
.render_parent_ref(based_on)
}
\description{
Render the parent/inheritance positional argument shared by \code{defcolumn},
\code{defcrop}, and (for the inheriting-program case) \code{defprogram}: the bare
keyword \code{default} when no real parent is named (Daisy's own "use the
library default" convention, e.g. \verb{(defcolumn "X" default ...)} /
\verb{(defcrop "Spring Barley" default ...)}), or a quoted reference to
another named object otherwise (\verb{(defprogram "west" "Common" ...)}).
}
\keyword{internal}
