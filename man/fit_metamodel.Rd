% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metamodel.R
\name{fit_metamodel}
\alias{fit_metamodel}
\title{Fit a Kriging metamodel to a scored parameter design}
\usage{
fit_metamodel(
  scored_design,
  params,
  response_col = "score",
  covtype = "matern5_2",
  ...
)
}
\arguments{
\item{scored_design}{A \code{data.frame} with one column per parameter and a
scalar response column -- typically a \code{daisyr_scored_design} from
\code{\link[=score_param_design]{score_param_design()}}, but any table with the right columns works.}

\item{params}{Parameter bounds: a \code{data.frame}/list of \verb{\{name, min, max\}}, or a \code{daisyr_param_config} -- the same argument shape as
\code{\link[=generate_param_design]{generate_param_design()}}. Only \code{name} is used here, to pick and order
\code{scored_design}'s parameter columns; pass the same \code{params}/\code{config}
used to build the design for consistency.}

\item{response_col}{Character scalar naming \code{scored_design}'s scalar
response column (default \code{"score"}, matching \code{\link[=score_param_design]{score_param_design()}}'s
output).}

\item{covtype}{Covariance kernel passed to \code{DiceKriging::km()} (default
\code{"matern5_2"}, a common default for computer-experiment metamodels).}

\item{...}{Additional arguments forwarded to \code{DiceKriging::km()} (e.g.
\code{formula}, \code{nugget}, \code{estim.method}, \code{control}).}
}
\value{
A \code{daisyr_metamodel}: a list with \code{km} (the fitted \code{DiceKriging}
\code{km} object), \code{param_names}, \code{params} (normalised bounds), and
\code{response_col}.
}
\description{
Wraps \code{DiceKriging::km()}: fits a Gaussian-process surrogate mapping
parameter combinations to the scalar response \code{\link[=score_param_design]{score_param_design()}}
produced, so cheap predictions (with uncertainty) stand in for further
Daisy runs -- for sensitivity analysis, uncertainty bounds, or as the
starting model for \code{\link[=calibrate_daisy_ego]{calibrate_daisy_ego()}}.
}
\examples{
\dontrun{
metamodel <- fit_metamodel(scored, params, covtype = "matern5_2")
predict(metamodel, newdata = data.frame(Ap_clay = 0.08, LAIvsDS_L = 5))
}
}
\seealso{
\code{\link[=score_param_design]{score_param_design()}}, \code{\link[=validate_metamodel]{validate_metamodel()}}, \code{\link[=calibrate_daisy_ego]{calibrate_daisy_ego()}}
}
