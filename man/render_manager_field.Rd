% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dai_programs.R
\name{render_manager_field}
\alias{render_manager_field}
\title{Render a program's \verb{manager:} field. A single bare/quoted name (YAML:
\code{manager: "SBarley w. MF"}) renders as a plain \verb{(manager "SBarley w. MF")} -- a manager sourced directly from a library's own \code{defaction}
(or other manager model) definition, with no \code{activity} submodel
wrapper. A list (the usual case) renders via \code{render_manager_list()}'s
\verb{(manager activity ...)} form.}
\usage{
render_manager_field(manager, indent)
}
\description{
Render a program's \verb{manager:} field. A single bare/quoted name (YAML:
\code{manager: "SBarley w. MF"}) renders as a plain \verb{(manager "SBarley w. MF")} -- a manager sourced directly from a library's own \code{defaction}
(or other manager model) definition, with no \code{activity} submodel
wrapper. A list (the usual case) renders via \code{render_manager_list()}'s
\verb{(manager activity ...)} form.
}
\keyword{internal}
