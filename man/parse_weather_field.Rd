% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dai_programs.R
\name{parse_weather_field}
\alias{parse_weather_field}
\title{Inverse of \code{render_weather()}. A bare-string weather (\code{weather: none})
and a dict weather with only \code{method} and no \code{file} render identically
(\verb{(weather none)} either way), so a single bare-atom form always
reconstructs as the simpler bare-string shape -- byte-identical either
way, see this file's parsing header note pattern (dai_management.R).}
\usage{
parse_weather_field(children)
}
\description{
Inverse of \code{render_weather()}. A bare-string weather (\code{weather: none})
and a dict weather with only \code{method} and no \code{file} render identically
(\verb{(weather none)} either way), so a single bare-atom form always
reconstructs as the simpler bare-string shape -- byte-identical either
way, see this file's parsing header note pattern (dai_management.R).
}
\keyword{internal}
