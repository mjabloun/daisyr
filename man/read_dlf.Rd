% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_dlf.R
\name{read_dlf}
\alias{read_dlf}
\title{Read a Daisy log file (.dlf)}
\usage{
read_dlf(path, digits = NULL, cols = NULL)
}
\arguments{
\item{path}{Character scalar. Path to the \code{.dlf} file to read. The
function stops with an error if the file doesn't exist, is empty, has
no detectable header, or has a header but no data rows.}

\item{digits}{Integer scalar, or \code{NULL} (default). If \code{NULL}, values are
returned as read, with no rounding applied. If set, every numeric
column in the \emph{returned} table is rounded to this many decimal places
via \code{round()}, with one exception: the date/time columns (\code{Year},
\code{Month}, \code{MDay}/\code{Day}, \code{Hour} - matched case-insensitively, whichever
of these are present) are never rounded, even though they're numeric,
since rounding them would corrupt the date.}

\item{cols}{Character vector of column names, or \code{NULL} (default). If
\code{NULL}, all columns from the file are returned. If set, the returned
table is restricted to just these columns plus whichever date/time
columns exist in the file (\code{Year}/\code{Month}/\code{MDay}/\code{Day}/\code{Hour}) - the
date/time columns are always kept even if you don't list them, so
downstream helpers like \code{\link[=add_date]{add_date()}} still have what they need. Any
name in \code{cols} that isn't actually a column in the file is dropped
with a warning rather than causing an error. Column order in the
output follows the file's original order, not the order given in
\code{cols}.}
}
\value{
A \code{data.table} with one row per data line in the file, attributes
\code{"source_file"} (the \code{path}) and \code{"units"} (named character vector,
same names as the columns), and column names taken from the file's
header line.
}
\description{
Parses a Daisy \code{.dlf} output file into a \code{data.table}. Handles both the
standard format (a \verb{----------} separator line before the header) and
files written with \verb{print_header false} (no separator, so the header is
found as the first tab-separated line). Also copes with \verb{print_dimension false}, where the line right after the header is data rather than a
units row.
}
\details{
Column units from Daisy's dimension line (\verb{print_dimension true}, the
default) are stored as a \strong{named character vector} in \code{attr(x, "units")}.
Names match \code{names(x)}; columns with no unit (dates, identifiers, empty
cells) are \code{NA}. Prefer \code{\link[=get_unit]{get_unit()}} / \code{\link[=set_unit]{set_unit()}} over touching the
attribute directly. When the file has no dimension line, every entry is
\code{NA}.

A table-level attribute is used instead of renaming columns or attaching
a unit to each vector: it matches the existing \code{source_file} attribute,
does not break code that selects columns by name, and is subset with
\code{cols}. Daisy strings such as \code{"Mg DM/ha"} are not always valid SI, so
they are kept as plain text rather than converted to unit objects.
}
\examples{
\dontrun{
# Full table, no rounding
read_dlf("harvest.dlf")

# Full table, numeric columns rounded to 2 dp (date/time columns untouched)
read_dlf("harvest.dlf", digits = 2)

# Only WSOrg (plus date/time columns), unrounded
read_dlf("harvest.dlf", cols = c("WSOrg"))

# Only WSOrg (plus date/time columns), rounded to 2 dp
read_dlf("harvest.dlf", digits = 2, cols = c("WSOrg"))
}
}
\seealso{
\code{\link[=get_unit]{get_unit()}}, \code{\link[=set_unit]{set_unit()}}, \code{\link[=add_date]{add_date()}}
}
