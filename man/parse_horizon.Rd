% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dai_horizons.R
\name{parse_horizon}
\alias{parse_horizon}
\title{Parse a \verb{(defhorizon name texture [description] field...)} form's body
children (i.e. everything after the leading \code{defhorizon} keyword atom)
back into one \code{horizons[[i]]} entry -- the inverse of \code{render_horizon()},
also accepting a bare (unquoted) name and/or Daisy's optional positional
description string (see dai_parse_helpers.R). The block's own \code{comment}
field, if any, is attached by the caller (\code{parse_dai()}, dai_read.R),
not here -- see that file for why.}
\usage{
parse_horizon(body_children)
}
\description{
Parse a \verb{(defhorizon name texture [description] field...)} form's body
children (i.e. everything after the leading \code{defhorizon} keyword atom)
back into one \code{horizons[[i]]} entry -- the inverse of \code{render_horizon()},
also accepting a bare (unquoted) name and/or Daisy's optional positional
description string (see dai_parse_helpers.R). The block's own \code{comment}
field, if any, is attached by the caller (\code{parse_dai()}, dai_read.R),
not here -- see that file for why.
}
\keyword{internal}
