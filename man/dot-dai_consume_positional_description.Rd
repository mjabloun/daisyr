% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dai_parse_helpers.R
\name{.dai_consume_positional_description}
\alias{.dai_consume_positional_description}
\title{If the next sibling node is a bare/quoted string (not a \verb{(key value)}
field form), consume it as a positional \code{description}. Returns
\code{list(description = ..., rest = ...)} -- \code{description} is \code{NULL} (and
\code{rest} is \code{children} unchanged) when the next node isn't a string.}
\usage{
.dai_consume_positional_description(children)
}
\description{
If the next sibling node is a bare/quoted string (not a \verb{(key value)}
field form), consume it as a positional \code{description}. Returns
\code{list(description = ..., rest = ...)} -- \code{description} is \code{NULL} (and
\code{rest} is \code{children} unchanged) when the next node isn't a string.
}
\keyword{internal}
