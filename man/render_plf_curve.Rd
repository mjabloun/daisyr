% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/curves.R
\name{render_plf_curve}
\alias{render_plf_curve}
\title{Evaluate a built-in or custom PLF curve}
\usage{
render_plf_curve(curve, x, params)
}
\arguments{
\item{curve}{Character scalar. One of \code{\link[=known_plf_curves]{known_plf_curves()}}, or the name of
a curve registered via \code{\link[=register_plf_curve]{register_plf_curve()}}.}

\item{x}{Numeric vector of fixed x-knots.}

\item{params}{Numeric vector of shape parameters, given \emph{positionally} in
the order the curve expects (see Details) - a config's \code{plf_curves}
entry supplies these via its own arbitrarily-named \code{parameters} entries,
listed in \code{params} in the matching order, so the curve function itself
never needs to know those config-level names.}
}
\value{
Numeric vector, same length as \code{x}.
}
\description{
Evaluate a built-in or custom PLF curve
}
\details{
Built-in curve families and their expected parameter order, all monotonic
in \code{x} for sensible parameter ranges:
\describe{
\item{\code{logistic}}{\code{y = L / (1 + exp(-k * (x - x0)))}; order \verb{(L, k, x0)}.}
\item{\code{gompertz}}{\code{y = L * exp(-b * exp(-k * x))}; order \verb{(L, b, k)}.}
\item{\code{richards}}{\code{y = L / (1 + exp(-k * (x - x0)))^(1 / v)}; order \verb{(L, k, x0, v)}.}
\item{\code{exponential}}{\code{y = a * exp(b * x)}; order \verb{(a, b)}.}
}
Custom curves registered via \code{\link[=register_plf_curve]{register_plf_curve()}} receive \code{params} as
the same plain (unnamed) numeric vector and are free to interpret it
however they like.
}
