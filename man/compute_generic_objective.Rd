% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/objective.R
\name{compute_generic_objective}
\alias{compute_generic_objective}
\title{Compare simulated Daisy output to observed data with a generic objective}
\usage{
compute_generic_objective(
  obs_data,
  sim_file,
  value_map,
  join_cols = "Date",
  sim_reader = read_dlf,
  sim_data = NULL,
  sim_mutator = NULL,
  metric = c("RMSE", "KGE", "MAE"),
  metric_fun = NULL,
  weights = NULL,
  return_per_pair = FALSE,
  plot = FALSE,
  plot_labels = list(x = "Observed", y = "Simulated")
)
}
\arguments{
\item{obs_data}{data.frame/data.table of observed values. Must contain the
\code{join_cols} plus every \code{obs_col} referenced in \code{value_map}.}

\item{sim_file}{Character scalar, optional. Path to the Daisy simulation
output to compare against. May be omitted when \code{sim_data} is supplied.}

\item{value_map}{List/data.frame describing observed vs simulated column
pairs (multi-objective definitions); see \code{\link[=normalise_value_map]{normalise_value_map()}}.}

\item{join_cols}{Character vector. Key column(s) used to align obs/sim
data (default \code{"Date"}); both tables must contain them.}

\item{sim_reader}{Function that reads \code{sim_file} and returns a table.
Defaults to \code{\link[=read_dlf]{read_dlf()}}, but any reader (e.g. \code{\link[data.table:fread]{data.table::fread()}})
works.}

\item{sim_data}{data.frame/data.table, optional. Already-loaded simulation
output (e.g. from \code{\link[=read_dlf]{read_dlf()}}). Useful when one simulation feeds
multiple objective calculations.}

\item{sim_mutator}{Optional function applied to the simulated table after
reading (e.g. to rename columns or derive metrics).}

\item{metric}{Character scalar, one of \code{"RMSE"}, \code{"KGE"}, \code{"MAE"}. Ignored
when \code{metric_fun} is supplied.}

\item{metric_fun}{Optional function \code{f(sim, obs)} returning a scalar
goodness-of-fit score; overrides \code{metric}.}

\item{weights}{Numeric vector, one per row of \code{value_map}. Defaults to the
\code{weight} column of \code{value_map} (or 1 for every pair).}

\item{return_per_pair}{If \code{TRUE}, also return a table with each
objective's score and weight.}

\item{plot}{If \code{TRUE}, build a scatter/fit plot for every objective pair
(requires ggplot2).}

\item{plot_labels}{List with \code{x}/\code{y} entries used to label the plots.}
}
\value{
A list with \code{summary} (the weighted-mean score across all pairs),
and optionally \code{per_variable} and \code{plot}.
}
\description{
Generalises crop/site-specific cost functions (e.g. an ad hoc
\code{f.ThetaCost()}) into a single, reusable API: join observed and simulated
tables on shared key column(s), score one or more column pairs with a
goodness-of-fit metric, and combine them into a single weighted score.
}
\examples{
\dontrun{
value_map <- list(
  list(obs_col = "Theta1", sim_col = "simTheta1", label = "Top layer")
)
compute_generic_objective(
  obs_data = obs.swc, sim_file = "Output/interval_water_content.dlf",
  value_map = value_map, join_cols = "Date", metric = "KGE"
)
}
}
