% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plf_io.R
\name{read_plf}
\alias{read_plf}
\title{Read a piecewise linear function (PLF) parameter from a .dai file}
\usage{
read_plf(file, parameter, occurrence = 1)
}
\arguments{
\item{file}{Character scalar. Path to the \code{.dai} file.}

\item{parameter}{Character scalar. Name of the PLF parameter to extract
(e.g. \code{"LAIvsDS"}).}

\item{occurrence}{Integer. Which occurrence of \code{parameter} to extract, if
it appears more than once in the file (e.g. once per crop/horizon
definition). Defaults to the first.}
}
\value{
A \code{data.frame} with columns \code{x} and \code{y}, one row per point, in
the order they appear in the file.
}
\description{
Extracts the \verb{(x y) (x y) ...} sequence of a named PLF parameter directly
from an existing \code{.dai} file - e.g. to inspect a value already in a
template, or to seed \code{x_values}/starting shape parameters for a
\code{plf_curves} config entry. The parameter's value may span multiple
lines and may contain end-of-line comments (\verb{;} to end of line), both of
which are handled before parsing.
}
\examples{
\dontrun{
read_plf("dk-sbarley.dai", "TempEff1")
}
}
