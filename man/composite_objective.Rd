% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/objective.R
\name{composite_objective}
\alias{composite_objective}
\title{Combine named component objectives into one weighted score}
\usage{
composite_objective(components, weights = NULL, name = NULL)
}
\arguments{
\item{components}{Named list of \code{daisyr_objective} objects. Names are tags
(e.g. \code{"yield"}, \code{"swc"}).}

\item{weights}{Numeric vector, one per component (default 1).}

\item{name}{Optional character scalar stored on the object.}
}
\value{
An object of class \code{daisyr_composite_objective} (also
\code{daisyr_objective}).
}
\description{
Each component is a \code{\link[=objective_spec]{objective_spec()}} (one observation table, join,
metric, and optional \code{output_file}). After a Daisy run, \code{\link[=evaluate_objective]{evaluate_objective()}}
scores every component against its own \code{.dlf} (resolved next to \code{sim_file})
and returns the weighted mean. Use this when yield, biomass, and SWC live
in different files or need different joins/metrics.
}
