% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dai_parse_helpers.R
\name{.dai_parse_generic_block_body}
\alias{.dai_parse_generic_block_body}
\title{Parse every \verb{(key ...)} sibling of a block body into a named list -- the
inverse of \code{render_generic_block_body()}. Every sibling must be a
\verb{(key ...)} field form. A \verb{;;}/\verb{;} comment interleaved between fields
(or at the end of a field's own line) is recovered on \code{field_comments}
keyed by the field it precedes (or, for an end-of-line comment, the
field it shares a line with), and regenerated immediately before that
field. Any other unexpected node raises a clear error rather than being
silently skipped, per this file's scope note.}
\usage{
.dai_parse_generic_block_body(nodes)
}
\description{
Parse every \verb{(key ...)} sibling of a block body into a named list -- the
inverse of \code{render_generic_block_body()}. Every sibling must be a
\verb{(key ...)} field form. A \verb{;;}/\verb{;} comment interleaved between fields
(or at the end of a field's own line) is recovered on \code{field_comments}
keyed by the field it precedes (or, for an end-of-line comment, the
field it shares a line with), and regenerated immediately before that
field. Any other unexpected node raises a clear error rather than being
silently skipped, per this file's scope note.
}
\keyword{internal}
