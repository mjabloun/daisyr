% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dai_helpers.R
\name{render_condition}
\alias{render_condition}
\title{Render a \code{date}/\code{condition} spec to its Daisy predicate text, e.g.
\verb{(crop_ds_after "Spring Barley" 2.0 [])} or
\verb{(or (crop_ds_after "X" 1.8 [])\\n    (mm_dd 10 1))}.
Does NOT wrap in \verb{(wait ...)} -- see \code{render_wait_from_date()} for that.}
\usage{
render_condition(spec, default_crop = NULL)
}
\description{
Understands both the user-facing shorthand (a bare \code{"MM-DD"} string, or
\code{ds}/\code{mm_dd}/\code{days}/\code{any_of}/\code{all_of}/\code{not}/\code{crop_dm_over}/
\code{soil_water_pressure_above}/\code{raw}) and, recursively, its own expansion
of that shorthand (\code{crop_ds_after}) -- so \code{any_of}/\code{all_of}/\code{not} can
nest either form freely. \code{default_crop} (typically the enclosing
operation's own \code{crop} field) fills in \code{{ds: 1.8}} when it doesn't name
a crop explicitly, and is threaded through nested conditions too.
}
\keyword{internal}
