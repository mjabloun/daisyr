% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/param_design.R
\name{generate_param_design}
\alias{generate_param_design}
\title{Space-filling parameter design for a Daisy metamodel}
\usage{
generate_param_design(
  params,
  n,
  method = c("lhs", "dice_lhs", "sobol"),
  seed = NULL,
  ...
)
}
\arguments{
\item{params}{A \code{data.frame} with columns \code{name}, \code{min}, \code{max} (one row
per parameter), or a list of \verb{\{name, min, max\}} entries. Named list
elements may omit \code{name}. A \code{daisyr_param_config} is also accepted (bounds
taken from its \code{parameters} table).}

\item{n}{Number of design points (rows) to generate.}

\item{method}{\code{"lhs"}, \code{"dice_lhs"}, or \code{"sobol"}.}

\item{seed}{Optional integer. If given, \code{set.seed(seed)} is called before
sampling (covering the \code{"dice_lhs"} optimization pass too, since
\code{DiceDesign::maximinESE_LHS()} has no seed argument of its own), and
for Sobol' the same value is passed as \code{randtoolbox::sobol()}'s
scrambling seed so the sequence is reproducible and
\code{\link[=extend_param_design]{extend_param_design()}} can continue it.}

\item{...}{Forwarded to \code{DiceDesign::maximinESE_LHS()} when
\code{method = "dice_lhs"}; ignored otherwise.}
}
\value{
A \code{data.frame} (class \code{daisyr_param_design}) with one column per
parameter and \code{n} rows. Attributes \code{params}, \code{method}, \code{seed}, and
\code{n_drawn} are attached for \code{\link[=extend_param_design]{extend_param_design()}}.
}
\description{
Draws \code{n} points in the parameter hyper-rectangle defined by \code{params},
intended as the training design for a surrogate / emulator of Daisy.
Dimension \code{k} is whatever \code{params} contains (one parameter or many); it
is not hardcoded.
}
\details{
\describe{
\item{\code{method = "lhs"}}{An optimized Latin hypercube via
\code{lhs::maximinLHS()}. That is the general-purpose choice: its cost
stays reasonable as \code{n} or \code{k} grows. When \code{n * k} is small enough
that \code{lhs::optimumLHS()}'s simulated-annealing search is practical,
that function typically yields a slightly better maximin design;
it is not used here because its runtime grows quickly.}
\item{\code{method = "dice_lhs"}}{A Latin hypercube built with
\code{DiceDesign::lhsDesign()} and then optimized with
\code{DiceDesign::maximinESE_LHS()}'s Enhanced Stochastic Evolutionary
algorithm. For a fixed, one-shot batch this typically reaches a
better maximin spacing than \code{"lhs"} for the same \code{n}/\code{k}, at the
cost of a heavier optimization pass -- worth it when the design is
small and every Daisy run is expensive, which is the usual
metamodel-training situation. \code{...} is forwarded to
\code{maximinESE_LHS()} (e.g. \code{it}, \code{inner_it}, \code{J}, \code{p}, \code{T0}) to tune
that search; the defaults from \code{DiceDesign} are used otherwise. Like
\code{"lhs"}, the result is not extensible -- see \code{\link[=extend_param_design]{extend_param_design()}}.}
\item{\code{method = "sobol"}}{A Sobol' low-discrepancy sequence via
\code{randtoolbox::sobol(n, dim = k, scrambling = 3)} (Owen + Faure-Tezuka
scrambling when the installed randtoolbox supports it). Recent
randtoolbox versions temporarily disable scrambling; the call is
unchanged so it will pick scrambling back up when the package
re-enables it. Sobol' designs can be extended later with
\code{\link[=extend_param_design]{extend_param_design()}} without rebuilding the existing points.}
}

All three methods draw in the unit hypercube and are then rescaled to
each parameter's \verb{[min, max]}. \code{lhs}, \code{DiceDesign}, and \code{randtoolbox} are
optional dependencies (in Suggests); only the package backing the
requested \code{method} is required.
}
\examples{
\dontrun{
params <- data.frame(
  name = c("Ap_clay", "LAIvsDS_L"),
  min = c(0.05, 3),
  max = c(0.10, 7)
)
generate_param_design(params, n = 20, method = "lhs", seed = 1)
generate_param_design(params, n = 20, method = "dice_lhs", seed = 1)
generate_param_design(params, n = 20, method = "sobol", seed = 1)
}
}
\seealso{
\code{\link[=extend_param_design]{extend_param_design()}}, \code{\link[=run_param_design]{run_param_design()}}
}
