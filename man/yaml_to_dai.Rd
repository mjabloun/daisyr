% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dai_generate.R
\name{yaml_to_dai}
\alias{yaml_to_dai}
\title{Generate a Daisy \code{.dai} file from a YAML config}
\usage{
yaml_to_dai(yaml_path, output_path = NULL)
}
\arguments{
\item{yaml_path}{Character scalar. Path to the input YAML file.}

\item{output_path}{Character scalar, or \code{NULL} (default). If given, the
generated text is also written to this path (via \code{writeLines()}).}
}
\value{
The generated \code{.dai} text, as a character scalar, invisibly.
}
\description{
Reads a config in the YAML schema documented at the top of this file
(and, per-section, in dai_management.R / dai_horizons.R / dai_columns.R
/ dai_crops.R / dai_programs.R) and renders it to Daisy's \code{.dai}
s-expression syntax: \code{directory}/\code{path}/library includes, \code{defhorizon},
\code{defcolumn}, \code{defcrop}, \code{defaction}, and \code{defprogram} blocks, and
standalone \code{run} statements, in that order. Every section is optional.
}
\details{
Building \code{config} yourself instead of writing YAML -- as a plain R
list, or from a data.frame via \code{records_from_df()} (dai_dataframe.R) --
and calling \code{generate_dai()} directly is an equally supported way to
use this generator; \code{yaml_to_dai()} is just the YAML-file-shaped
convenience wrapper around it. See \code{generate_dai()}'s own
documentation.
}
\examples{
\dontrun{
yaml_to_dai("cauliflower-management.yaml", "cauliflower-man.dai")
cat(yaml_to_dai("west-soil-column.yaml"))
}
}
