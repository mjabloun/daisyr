% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibrate.R
\name{calibrate_daisy}
\alias{calibrate_daisy}
\title{Calibrate Daisy parameters against observed data}
\usage{
calibrate_daisy(
  config,
  run_file,
  daisy_exe = NULL,
  objective,
  sim_file,
  working_dir = NULL,
  template_dir = ".",
  output_dir = ".",
  show_log = FALSE,
  maximize = FALSE,
  method = "DEoptim",
  control = list(),
  reporter = NULL,
  cmd = NULL,
  names = NULL
)
}
\arguments{
\item{config}{A \code{daisyr_param_config} object.}

\item{run_file, daisy_exe, working_dir, show_log, cmd}{Passed to \code{\link[=run_daisy]{run_daisy()}}.
\code{daisy_exe} and \code{cmd} may be \code{NULL} to use \code{\link[=get_daisy_path]{get_daisy_path()}} / the
default local command.}

\item{objective}{A \code{daisyr_objective}, as returned by \code{\link[=objective_spec]{objective_spec()}}.}

\item{sim_file}{Character scalar. Path to the Daisy output file the
objective should be evaluated against (relative to \code{working_dir} if
Daisy was run there).}

\item{template_dir, output_dir}{Passed to \code{\link[=render_templates]{render_templates()}}.}

\item{maximize}{If \code{TRUE}, the returned score is \code{-summary} so that
minimizers (like \code{DEoptim}) can be used to maximize a metric such as KGE.}

\item{method}{\code{"DEoptim"}, \code{"BOBYQA"}, \code{"multi-BOBYQA"}, \code{"DDS"}, or \code{"CMA-ES"}.}

\item{control}{Method-specific list.}

\item{reporter}{Optional function called after every candidate evaluation.
It receives \code{evals}, \code{iter}, \code{np}, \code{n_total}, \code{generation_end},
\code{last_score}, \code{best_score}, and \code{best_params}.}

\item{names}{Character vector of config names to vary. Others stay at
config defaults. \code{NULL} means every name. PLF shape parameters are
expanded to whole curves via \code{\link[=expand_calibrate_names]{expand_calibrate_names()}}.}
}
\value{
A list with \code{best_params}, \code{fitted_names}, \code{best_score}, \code{method},
and \code{fit}.
}
\description{
Fits \code{names} (or every name in \code{\link[=param_config_names]{param_config_names()}}) against
\code{\link[=evaluate_daisy_candidate]{evaluate_daisy_candidate()}}. Bounds come from the config \code{min} / \code{max}.
}
\details{
\describe{
\item{\code{DEoptim}}{Differential evolution. Control: \code{NP}, \code{itermax}.}
\item{\code{BOBYQA}}{\code{minqa::bobyqa}. Control: \code{maxfun} / \code{maxeval}, \code{start}.}
\item{\code{multi-BOBYQA}}{Several BOBYQA runs. Control: \code{nstarts} (default 8),
\code{seed}, \code{include_default}, plus BOBYQA keys.}
\item{\code{DDS}}{Dynamically Dimensioned Search (in-package). Control:
\code{maxeval}, \code{r}, \code{seed}, \code{start}.}
\item{\code{CMA-ES}}{\code{cmaes::cma_es}. Control: \code{maxit} / \code{maxeval}, \code{sigma},
\code{start}.}
}
}
