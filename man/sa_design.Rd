% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensitivity.R
\name{sa_design}
\alias{sa_design}
\title{Build a Morris or Sobol-Jansen design from free parameters}
\usage{
sa_design(
  config,
  method = c("morris", "sobol"),
  r = 10,
  design = list(type = "oat", levels = 5, grid.jump = 3),
  N = 1000,
  scale = TRUE,
  seed = NULL
)
}
\arguments{
\item{config}{A \code{daisyr_param_config} object.}

\item{method}{\code{"morris"} or \code{"sobol"} (Sobol-Jansen).}

\item{r}{Number of trajectories (Morris only).}

\item{design}{List passed to \code{sensitivity::morris()}'s \code{design} argument
(Morris only).}

\item{N}{Base sample size per parameter (Sobol only).}

\item{scale}{Whether \code{min}/\code{max} should be used for scaling.}

\item{seed}{Optional seed for reproducibility.}
}
\value{
A \code{morris}/\code{sobol} object (from the \code{sensitivity} package) whose
\verb{$X} matrix contains the parameter sets to evaluate, with \code{min}/\code{max}
ranges attached as an \code{"config"} attribute for convenience.
}
\description{
Wraps \code{sensitivity::morris()}/\code{sensitivity::soboljansen()} in "decoupled"
mode (\code{model = NULL}), using every free parameter in \code{config}
(\code{\link[=param_config_names]{param_config_names()}}) - i.e. direct parameters and \code{plf_curves}
shape parameters alike - as the design columns, with bounds taken from
the config's \code{min}/\code{max} columns. Decoupled mode is used because Daisy
runs are expensive external processes, not plain R functions: the design
matrix is generated up front, evaluated externally with \code{\link[=run_sa_design]{run_sa_design()}},
and the responses are attached afterwards with \code{\link[=complete_sa_analysis]{complete_sa_analysis()}}.
}
