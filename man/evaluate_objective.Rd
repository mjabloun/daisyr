% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/objective.R
\name{evaluate_objective}
\alias{evaluate_objective}
\title{Evaluate a \code{daisyr_objective} against a simulation output file}
\usage{
evaluate_objective(objective, sim_file, sim_outputs = NULL, ...)
}
\arguments{
\item{objective}{A \code{daisyr_objective}, as returned by \code{\link[=objective_spec]{objective_spec()}}
or \code{\link[=composite_objective]{composite_objective()}}.}

\item{sim_file}{Character scalar. Path to a Daisy output from this run.
For a composite objective, each component's \code{output_file} is resolved
in the same directory (so yield and SWC logs from one run are found
together).}

\item{sim_outputs}{Optional named list of already-loaded tables (as from
a sensitivity \code{response_fun}). Used when the file path is not available.}

\item{...}{Passed on to \code{\link[=compute_generic_objective]{compute_generic_objective()}} (e.g.
\code{return_per_pair = TRUE}).}
}
\value{
See \code{\link[=compute_generic_objective]{compute_generic_objective()}}. Composites also include
\code{per_component}.
}
\description{
Evaluate a \code{daisyr_objective} against a simulation output file
}
