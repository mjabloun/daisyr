% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dai_read.R
\name{parse_dai}
\alias{parse_dai}
\title{Parse Daisy \code{.dai} text into the config list \code{generate_dai()} consumes}
\usage{
parse_dai(text)
}
\arguments{
\item{text}{Character scalar: the full text of a \code{.dai} file.}
}
\value{
A list in the schema documented at the top of dai_generate.R --
suitable to pass straight to \code{generate_dai()}.
}
\description{
The inverse of \code{generate_dai()}, bounded to exactly the same schema (see
this file's scope note and dai_parse_helpers.R's). Comments (\verb{;;} lines,
and \verb{;} end-of-line notes) are recovered wherever \code{generate_dai()} can
put one: a file-level \code{comment}, a block's own \code{comment},
\code{field_comments} inside a block, a comment on a library or \code{run} entry,
and \code{trailing_comment} at the end of the file. \verb{;;;} lines that are
\code{generate_dai()}'s own header are discarded, except that a
\verb{;;; Generated by generate_dai() from <file>} line (if present) is read
back into \code{config$source_file}, matching what \code{generate_dai()} writes
there from \code{yaml_to_dai()}. Other \verb{;;;} banners are kept as comments.
}
\details{
This is the pure, text-in/list-out worker -- the counterpart to
\code{generate_dai()}'s pure list-in/text-out. To parse a \code{.dai} \emph{file}
instead of text you already have in hand, use \code{read_dai()}, the
file-reading wrapper around this function (matching how \code{yaml_to_dai()}
is the YAML-file-reading wrapper around \code{generate_dai()}).

Unrecognized top-level Daisy (\code{deflog}, \code{defchemical}, ...) is kept as
\code{daisy_script} entries (original source text) and written back as-is by
\code{generate_dai()}. Unrecognized fields inside a \code{defprogram} (e.g.
\code{print_time}) are kept the same way on that program. If a \emph{recognized}
construct is malformed (a \code{defhorizon} with no texture class, an
unterminated \code{(}, ...), \code{parse_dai()} keeps going and collects every
such problem across the whole file before raising a single error
listing them all -- one line each, in file order, naming the source
line number it starts on.
}
\examples{
\dontrun{
config <- parse_dai(readLines("west.dai", warn = FALSE) |> paste(collapse = "\n"))
config$programs[[1]]$log_prefix <- "revised_"
cat(generate_dai(config))
}
}
