% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/param_config.R
\name{validate_param_config}
\alias{validate_param_config}
\title{Validate a config against the actual template files on disk}
\usage{
validate_param_config(config, template_dir = ".")
}
\arguments{
\item{config}{A \code{daisyr_param_config} object.}

\item{template_dir}{Character scalar. Directory that \code{from_file} paths in
the config are relative to. Defaults to the current directory.}
}
\value{
Invisibly \code{TRUE} if every check passes; otherwise throws an error
listing every problem found (not just the first one).
}
\description{
In addition to the structural checks in \code{\link[=read_param_config]{read_param_config()}}, this
confirms every declared placeholder actually exists - exactly once - in
its \code{from_file}, catching typos or drifted templates before any Daisy run
is attempted.
}
