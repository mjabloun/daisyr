% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/template.R
\name{build_substitutions}
\alias{build_substitutions}
\title{Render all substitutions implied by a config for a given parameter vector}
\usage{
build_substitutions(config, values)
}
\arguments{
\item{config}{A \code{daisyr_param_config} object.}

\item{values}{Named numeric vector covering every name in
\code{\link[=param_config_names]{param_config_names()}}.}
}
\value{
A list with one element per output file-pair (keyed by
\code{"from_file -> to_file"}), each itself a named list of placeholder ->
replacement text ready to hand to \code{\link[=render_one_template]{render_one_template()}}.
}
\description{
Given a full set of current values for every free parameter in \code{config}
(\code{\link[=param_config_names]{param_config_names()}}), computes: (a) the direct placeholder
substitutions, and (b) the rendered \verb{(x y)} text block for every
\code{plf_curves} entry (by evaluating its curve at the fixed \code{x_values} using
the current values of its shape parameters).
}
\keyword{internal}
