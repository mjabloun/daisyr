% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dai_management.R
\name{parse_activity}
\alias{parse_activity}
\title{Parse a \verb{(defaction name activity op...)} form's body children (i.e.
everything after the leading \code{defaction} keyword atom) back into one
\code{activities[[i]]} entry -- the inverse of \code{render_activity()}, also
accepting a bare (unquoted) name (see dai_parse_helpers.R). The
activity's own \code{comment} field, if any, is attached by the caller
(\code{parse_dai()}, dai_read.R), not here -- see that file for why.}
\usage{
parse_activity(body_children)
}
\description{
Parse a \verb{(defaction name activity op...)} form's body children (i.e.
everything after the leading \code{defaction} keyword atom) back into one
\code{activities[[i]]} entry -- the inverse of \code{render_activity()}, also
accepting a bare (unquoted) name (see dai_parse_helpers.R). The
activity's own \code{comment} field, if any, is attached by the caller
(\code{parse_dai()}, dai_read.R), not here -- see that file for why.
}
\keyword{internal}
