% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dai_dataframe.R
\name{.fold_value_unit_pairs}
\alias{.fold_value_unit_pairs}
\title{Fold \verb{<field>_value} / \verb{<field>_unit} column pairs in one row-list back
into \verb{<field> = list(value = ..., unit = ...)}, matching the shape
\code{render_generic_value()} (dai_helpers.R) expects. Only exact pairs are
folded; an unpaired \verb{*_value} or \verb{*_unit} column is left as-is. If the
\verb{_value} half is \code{NA}, the whole pair is dropped (see file header).}
\usage{
.fold_value_unit_pairs(row)
}
\description{
Fold \verb{<field>_value} / \verb{<field>_unit} column pairs in one row-list back
into \verb{<field> = list(value = ..., unit = ...)}, matching the shape
\code{render_generic_value()} (dai_helpers.R) expects. Only exact pairs are
folded; an unpaired \verb{*_value} or \verb{*_unit} column is left as-is. If the
\verb{_value} half is \code{NA}, the whole pair is dropped (see file header).
}
\keyword{internal}
