% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metamodel.R
\name{score_param_design}
\alias{score_param_design}
\title{Score every run of a parameter design against an objective}
\usage{
score_param_design(
  run,
  objective,
  output_name = NULL,
  output_files = NULL,
  maximize = FALSE,
  return_per_pair = FALSE
)
}
\arguments{
\item{run}{A list with a \code{design} \code{data.frame} (\code{run_id} + parameter
columns) and either \code{keep_dir} (archived files) or \code{outputs}
(in-memory tables) -- i.e. the return value of \code{\link[=run_param_design]{run_param_design()}} or
\code{\link[=read_param_design]{read_param_design()}}.}

\item{objective}{A \code{daisyr_objective}, as returned by \code{\link[=objective_spec]{objective_spec()}}.}

\item{output_name}{Character scalar. Which of \code{output_files}/\code{run$outputs}
the objective should be evaluated against -- the same names used in
\code{\link[=run_param_design]{run_param_design()}}'s \code{output_files} argument (e.g. \code{"harvest"}).
Optional when the objective (or its components) already set
\code{output_file}, or when \code{run$outputs} is a single stacked \code{data.frame}.
Still required for archived runs whose objective has no \code{output_file}.}

\item{output_files}{Named character vector of the \emph{original} Daisy
output paths, only needed when \code{run} is an archived run and doesn't
already carry a \code{manifest.rds} (i.e. a hand-built \code{list(design=..., keep_dir=...)} rather than \code{\link[=run_param_design]{run_param_design()}}'s own return value).
Ignored for in-memory runs.}

\item{maximize}{If \code{TRUE}, the stored \code{score} is negated -- so it stays
on the same "smaller is better" scale \code{\link[=fit_metamodel]{fit_metamodel()}} and
\code{\link[=calibrate_daisy_ego]{calibrate_daisy_ego()}} expect, and matches
\code{\link[=evaluate_daisy_candidate]{evaluate_daisy_candidate()}}'s own \code{maximize} convention. Set this the
same way you would set \code{maximize} on \code{\link[=calibrate_daisy]{calibrate_daisy()}} /
\code{\link[=calibrate_daisy_ego]{calibrate_daisy_ego()}} for the same objective.}

\item{return_per_pair}{If \code{TRUE}, also keep each run's per-variable
scores (see \code{\link[=compute_generic_objective]{compute_generic_objective()}}'s \code{return_per_pair}) as a
\code{per_variable} attribute on the result, named by \code{run_id}.}
}
\value{
\code{run$design} with one appended \code{score} column (class
\code{daisyr_scored_design}). An \code{objective} attribute is attached so
\code{\link[=fit_metamodel]{fit_metamodel()}} doesn't need it passed again.
}
\description{
The glue between \code{\link[=run_param_design]{run_param_design()}} (or \code{\link[=read_param_design]{read_param_design()}}) and
\code{\link[=fit_metamodel]{fit_metamodel()}}: \code{\link[=compute_generic_objective]{compute_generic_objective()}} already reduces one
simulation to a single scalar score (\verb{$summary}), this just loops that
over every row of a design and attaches the result as a \code{score} column,
producing the \verb{(X, y)} shape a metamodel needs.
}
\details{
Works against either form a design run can take. If \code{run$outputs} is
present (in-memory tables from \code{read = TRUE} or \code{\link[=read_param_design]{read_param_design()}}),
each \code{run_id} is scored by passing that slice as
\code{compute_generic_objective()}'s \code{sim_data}. Otherwise an archived run
(\code{keep_dir} from \code{\link[=run_param_design]{run_param_design()}} with \code{keep_files = TRUE}) is scored
file-by-file via \code{\link[=evaluate_objective]{evaluate_objective()}}. Either way you get back \code{design}
(the same table \code{\link[=run_param_design]{run_param_design()}} returned) plus one new \code{score}
column. When both \code{outputs} and \code{keep_dir} are present, the in-memory
tables are used so any \code{sim_mutator} already applied at read time is kept.
}
\examples{
\dontrun{
output_files <- c(harvest = "Output/harvest.dlf")
run <- run_param_design(
  design, config, run_file, daisy_exe,
  output_files = output_files, working_dir = "path/to/scenario",
  keep_files = TRUE, keep_dir = "metamodel_runs"
)
objective <- objective_spec(
  obs_data = obs.yield,
  value_map = list(list(obs_col = "yield", sim_col = "WSOrg")),
  metric = "RMSE"
)
scored <- score_param_design(run, objective, output_name = "harvest")
scored$score
}
}
\seealso{
\code{\link[=fit_metamodel]{fit_metamodel()}}, \code{\link[=run_param_design]{run_param_design()}}, \code{\link[=objective_spec]{objective_spec()}}
}
