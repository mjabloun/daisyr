% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run.R
\name{run_daisy}
\alias{run_daisy}
\title{Run the Daisy executable on a \code{.dai} setup file}
\usage{
run_daisy(
  run_file,
  daisy_exe = NULL,
  working_dir = NULL,
  show_log = FALSE,
  cmd = NULL
)
}
\arguments{
\item{run_file}{Character scalar. Path to the \code{.dai} file to run.
Relative paths are resolved against \code{working_dir} if given.}

\item{daisy_exe}{Character scalar path to \code{daisy.exe}, or \code{NULL}
(default) to use \code{\link[=get_daisy_path]{get_daisy_path()}}. Ignored when \code{cmd} is set and
does not contain \code{{daisy_exe}}.}

\item{working_dir}{Character scalar or \code{NULL}. If given, the working
directory is temporarily changed to this directory for the duration of
the call (and restored afterwards, even on error), matching Daisy's
convention of resolving \verb{(directory ...)}/input paths relative to the
current working directory. Also available as \code{{working_dir}} in \code{cmd}.}

\item{show_log}{If \code{TRUE}, stream Daisy's console output; otherwise it is
captured/discarded.}

\item{cmd}{Optional command template, interpolated with
\code{\link[glue:glue]{glue::glue()}}. Substitutions: \code{{run_file}}, \code{{daisy_exe}},
\code{{working_dir}}. If \code{NULL}, runs \verb{"\{daisy_exe\}" "\{run_file\}"}.
Shell snippets such as \verb{$(pwd)} are left for the shell. Example:
\code{"singularity run --bind $(pwd):$(pwd) ~/daisy/daisy.sif -q $(pwd)/{run_file}"}.}
}
\value{
Integer exit/status code from \code{\link[=system]{system()}} (invisibly). Non-zero
indicates Daisy reported an error - the caller decides whether that
should halt a calibration/SA run or just be logged.
}
\description{
Thin wrapper around \code{\link[=system]{system()}} that runs a Daisy \code{.dai} file. By default
this invokes a local \code{daisy.exe} (see \code{\link[=get_daisy_path]{get_daisy_path()}}). Pass \code{cmd} to
use another launcher (Singularity, a scheduler wrapper, ...).
}
\examples{
\dontrun{
run_daisy("test-optim.dai", working_dir = ".")

run_daisy(
  "test-optim.dai",
  working_dir = ".",
  cmd = "singularity run --bind $(pwd):$(pwd) ~/daisy/daisy.sif -q $(pwd)/{run_file}"
)
}
}
\seealso{
\code{\link[=set_daisy_path]{set_daisy_path()}}, \code{\link[=get_daisy_path]{get_daisy_path()}}
}
