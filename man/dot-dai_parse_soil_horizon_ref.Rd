% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dai_columns.R
\name{.dai_parse_soil_horizon_ref}
\alias{.dai_parse_soil_horizon_ref}
\title{Parse one entry of a \code{horizons} reference list -- accepts both the
fixed \verb{(depth [unit] "name")} shape \code{render_soil_block()} always
produces, and Daisy's own more flexible \verb{(depth name)} shape (the unit
defaults to "cm" when omitted, matching \code{render_soil_block()}'s own
\verb{\%||\% "cm"} default), with the horizon name as either a bare symbol or
a quoted string.}
\usage{
.dai_parse_soil_horizon_ref(ref)
}
\description{
Parse one entry of a \code{horizons} reference list -- accepts both the
fixed \verb{(depth [unit] "name")} shape \code{render_soil_block()} always
produces, and Daisy's own more flexible \verb{(depth name)} shape (the unit
defaults to "cm" when omitted, matching \code{render_soil_block()}'s own
\verb{\%||\% "cm"} default), with the horizon name as either a bare symbol or
a quoted string.
}
\keyword{internal}
