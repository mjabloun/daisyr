% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dai_programs.R
\name{parse_program}
\alias{parse_program}
\title{Parse a \verb{(defprogram name type-or-parent [description] field...)}
form's body children (i.e. everything after the leading \code{defprogram}
keyword atom) back into one \code{programs[[i]]} entry -- the inverse of
\code{render_program()}, also accepting a bare (unquoted) name and/or
Daisy's optional positional description string (see
dai_parse_helpers.R). Dispatches on the bare \code{Daisy}/\code{batch} keyword
vs. a parent reference, exactly as \code{render_program()} dispatches the
other way. The block's own \code{comment} field, if any, is attached by the
caller (\code{parse_dai()}, dai_read.R), not here.}
\usage{
parse_program(body_children)
}
\description{
Parse a \verb{(defprogram name type-or-parent [description] field...)}
form's body children (i.e. everything after the leading \code{defprogram}
keyword atom) back into one \code{programs[[i]]} entry -- the inverse of
\code{render_program()}, also accepting a bare (unquoted) name and/or
Daisy's optional positional description string (see
dai_parse_helpers.R). Dispatches on the bare \code{Daisy}/\code{batch} keyword
vs. a parent reference, exactly as \code{render_program()} dispatches the
other way. The block's own \code{comment} field, if any, is attached by the
caller (\code{parse_dai()}, dai_read.R), not here.
}
\keyword{internal}
