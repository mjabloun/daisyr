% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dai_columns.R
\name{parse_column}
\alias{parse_column}
\title{Parse a \verb{(defcolumn name parent [description] field...)} form's body
children (i.e. everything after the leading \code{defcolumn} keyword atom)
back into one \code{columns[[i]]} entry -- the inverse of \code{render_column()},
also accepting a bare (unquoted) name and/or Daisy's optional
positional description string (see dai_parse_helpers.R). The block's
own \code{comment} field, if any, is attached by the caller (\code{parse_dai()},
dai_read.R), not here.}
\usage{
parse_column(body_children)
}
\description{
Parse a \verb{(defcolumn name parent [description] field...)} form's body
children (i.e. everything after the leading \code{defcolumn} keyword atom)
back into one \code{columns[[i]]} entry -- the inverse of \code{render_column()},
also accepting a bare (unquoted) name and/or Daisy's optional
positional description string (see dai_parse_helpers.R). The block's
own \code{comment} field, if any, is attached by the caller (\code{parse_dai()},
dai_read.R), not here.
}
\keyword{internal}
