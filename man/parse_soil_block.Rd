% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dai_columns.R
\name{parse_soil_block}
\alias{parse_soil_block}
\title{Parse a \verb{(Soil ...)} form's children (i.e. everything after the leading
\code{Soil} keyword atom) back into \code{cfg$Soil} -- the inverse of
\code{render_soil_block()}, generalized to also accept Daisy's own more
flexible Soil shape as seen in real hand-written \code{.dai} files:
\code{MaxRootingDepth}/\code{horizons}/other sub-blocks in any order, an optional
per-point unit on \code{horizons} entries, and any other recognized Soil
parameter (e.g. \code{border}) passed through generically rather than
rejected.}
\usage{
parse_soil_block(children)
}
\description{
Parse a \verb{(Soil ...)} form's children (i.e. everything after the leading
\code{Soil} keyword atom) back into \code{cfg$Soil} -- the inverse of
\code{render_soil_block()}, generalized to also accept Daisy's own more
flexible Soil shape as seen in real hand-written \code{.dai} files:
\code{MaxRootingDepth}/\code{horizons}/other sub-blocks in any order, an optional
per-point unit on \code{horizons} entries, and any other recognized Soil
parameter (e.g. \code{border}) passed through generically rather than
rejected.
}
\keyword{internal}
