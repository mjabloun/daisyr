% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metamodel.R
\name{calibrate_daisy_ego}
\alias{calibrate_daisy_ego}
\title{Surrogate-assisted calibration via Efficient Global Optimization (EGO)}
\usage{
calibrate_daisy_ego(
  metamodel,
  config,
  run_file,
  daisy_exe = NULL,
  objective,
  sim_file,
  nsteps = 10,
  working_dir = NULL,
  template_dir = ".",
  output_dir = ".",
  show_log = FALSE,
  maximize = FALSE,
  control = NULL,
  kmcontrol = NULL,
  cmd = NULL
)
}
\arguments{
\item{metamodel}{A \code{daisyr_metamodel} from \code{\link[=fit_metamodel]{fit_metamodel()}}, whose
\code{param_names} must match \code{param_config_names(config)}.}

\item{config}{A \code{daisyr_param_config} object.}

\item{run_file, daisy_exe, working_dir, show_log, cmd}{Passed to \code{\link[=run_daisy]{run_daisy()}}
(via \code{\link[=evaluate_daisy_candidate]{evaluate_daisy_candidate()}}). \code{daisy_exe} may be \code{NULL} to use
\code{\link[=get_daisy_path]{get_daisy_path()}}.}

\item{objective}{A \code{daisyr_objective}, as returned by \code{\link[=objective_spec]{objective_spec()}}
-- must be the same objective \code{metamodel}'s response was scored with.}

\item{sim_file}{Character scalar. Path to the Daisy output file the
objective is evaluated against for each new candidate (relative to
\code{working_dir} if Daisy is run there).}

\item{nsteps}{Number of new candidates to actually evaluate on Daisy
(i.e. number of EGO iterations).}

\item{template_dir, output_dir}{Passed to \code{\link[=render_templates]{render_templates()}}.}

\item{maximize}{If \code{TRUE}, matches \code{\link[=evaluate_daisy_candidate]{evaluate_daisy_candidate()}}'s
\code{maximize}: the real objective is negated before being handed to EGO
(which always minimizes), and the reported \code{best_score} is negated
back. Must match how \code{metamodel}'s training response was scored.}

\item{control, kmcontrol}{Forwarded to \code{DiceOptim::EGO.nsteps()} (control
of its internal \code{genoud} search and Kriging re-estimation,
respectively).}
}
\value{
A list with:
\itemize{
\item \code{best_params} -- named numeric vector, the best parameter
combination found across \emph{both} the design \code{metamodel} was
trained on and the \code{nsteps} new candidates EGO evaluated.
\item \code{best_score} -- its objective score (sign-corrected if
\code{maximize = TRUE}).
\item \code{fit} -- the raw \code{DiceOptim::EGO.nsteps()} result (\code{par},
\code{value}, \code{lastmodel}, ...), for diagnostics.
\item \code{metamodel} -- a new \code{daisyr_metamodel} wrapping \code{fit$lastmodel}
(refit on the original design plus the \code{nsteps} new points) --
pass this back into \code{calibrate_daisy_ego()} to run more steps.
}
}
\description{
An alternative to \code{\link[=calibrate_daisy]{calibrate_daisy()}}'s DEoptim: instead of calling
Daisy on every candidate a population-based search proposes,
\code{DiceOptim::EGO.nsteps()} uses the Kriging metamodel to pick, one at a
time, the single most promising next candidate (maximum Expected
Improvement), calls the \emph{real} Daisy objective only for that candidate
via \code{\link[=evaluate_daisy_candidate]{evaluate_daisy_candidate()}}, and refits the metamodel before
repeating. \code{nsteps} real Daisy runs, instead of DEoptim's whole
population every generation -- worth it once the initial
\code{\link[=score_param_design]{score_param_design()}} batch is paid for and Daisy calls are the
bottleneck.
}
\details{
\code{metamodel} must already be fit (via \code{\link[=fit_metamodel]{fit_metamodel()}}) on a
\code{\link[=score_param_design]{score_param_design()}} response using the \emph{same} \code{maximize} convention
passed here: \code{\link[=score_param_design]{score_param_design()}}'s scores, the metamodel trained on
them, and \code{evaluate_daisy_candidate()}'s new evaluations all need to be
on the same minimize-this scale, exactly as \code{\link[=calibrate_daisy]{calibrate_daisy()}} already
requires for DEoptim.
}
\examples{
\dontrun{
result <- calibrate_daisy_ego(
  metamodel, config, run_file, daisy_exe, objective, sim_file,
  nsteps = 10, working_dir = "path/to/scenario"
)
result$best_params
result$best_score

# Not converged yet -- keep going from where this left off:
more <- calibrate_daisy_ego(
  result$metamodel, config, run_file, daisy_exe, objective, sim_file,
  nsteps = 10, working_dir = "path/to/scenario"
)
}
}
\seealso{
\code{\link[=fit_metamodel]{fit_metamodel()}}, \code{\link[=calibrate_daisy]{calibrate_daisy()}}, \code{\link[=evaluate_daisy_candidate]{evaluate_daisy_candidate()}}
}
