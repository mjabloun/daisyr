% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/param_config.R
\name{plot_plf_curves}
\alias{plot_plf_curves}
\title{Plot example curves for the built-in PLF curve families}
\usage{
plot_plf_curves(
  x = seq(-1, 1, length.out = 200),
  params = .default_plf_curve_params(),
  curves = names(params)
)
}
\arguments{
\item{x}{Numeric vector of x-values to evaluate the curves at. Defaults
to a fine grid over \verb{[-1, 1]}.}

\item{params}{Named list of parameter vectors, one per curve family (see
\code{\link[=render_plf_curve]{render_plf_curve()}} for the parameter order each family expects).
Defaults to a representative example for each built-in family.}

\item{curves}{Character vector of curve names to plot; defaults to all
built-in families with an entry in \code{params}.}
}
\value{
Invisibly, a \code{data.frame} with columns \code{curve}, \code{x}, \code{y} for the
plotted points.
}
\description{
A quick visual reference for what each family in \code{\link[=known_plf_curves]{known_plf_curves()}}
looks like, useful when picking a \code{curve} for a \code{plf_curves} config
entry. Uses base graphics so it works without any plotting package
beyond what R ships with.
}
\examples{
plot_plf_curves()
}
