% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dai_helpers.R
\name{render_generic_block_body}
\alias{render_generic_block_body}
\title{Render every \code{key: value} pair of a generic parameter block (a named
list -- typically one nesting level of a \code{defcrop}/\code{defhorizon} entry,
or the contents of a \verb{[method, \{...\}]} pair / nested mapping) to a
single space-joined string of \verb{(key value)} forms, skipping the
reserved \code{comment} key (handled separately by callers via
\code{.with_comment()}, not passed through generically). Per-field comments
recovered by \code{parse_dai()} live on \code{field_comments} and are emitted
immediately before the matching \verb{(key ...)} form.}
\usage{
render_generic_block_body(fields, indent = "")
}
\description{
Render every \code{key: value} pair of a generic parameter block (a named
list -- typically one nesting level of a \code{defcrop}/\code{defhorizon} entry,
or the contents of a \verb{[method, \{...\}]} pair / nested mapping) to a
single space-joined string of \verb{(key value)} forms, skipping the
reserved \code{comment} key (handled separately by callers via
\code{.with_comment()}, not passed through generically). Per-field comments
recovered by \code{parse_dai()} live on \code{field_comments} and are emitted
immediately before the matching \verb{(key ...)} form.
}
\keyword{internal}
