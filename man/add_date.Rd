% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_dlf.R
\name{add_date}
\alias{add_date}
\title{Add a \code{Date} column to a Daisy data.table}
\usage{
add_date(dt, label)
}
\arguments{
\item{dt}{A \code{data.table}, typically the output of \code{\link[=read_dlf]{read_dlf()}}.}

\item{label}{Character scalar used to identify the source of \code{dt} in the
warning message when Year/Month/Day columns can't be found (e.g. the
name of the file \code{dt} was read from).}
}
\value{
\code{dt}, invisibly modified in place and returned, with a \code{Date}
column added (and an \code{hour} column, if an hour column was found).
}
\description{
Given a \code{data.table} with Year/Month/Day(/Hour) columns - under either
naming convention seen in Daisy output (e.g. \code{"Year"}/\code{"year"},
\code{"MDay"}/\code{"day"}) - builds a proper \code{Date} column from them. If an hour
column is present, its values are also copied into a \code{hour} column
(added for convenient, consistently-named access downstream, alongside
whatever the original hour column was called).
}
\details{
\code{dt} is modified in place (via \code{data.table}'s \verb{:=}) and returned. New
columns (\code{Date}, and \code{hour} when copied) get \code{NA} in \code{attr(dt, "units")}
when that attribute is already present.

If the Year/Month/Day columns can't all be identified, a warning is
issued (including \code{label} and the columns actually found, to make the
source easy to track down), a \code{Date} column of \code{NA}s is added so
downstream code can still assume the column exists, and the function
returns early without attempting to build a real date.
}
\examples{
\dontrun{
harvest_dt <- add_date(read_dlf("harvest.dlf", digits = 2), "harvest")
}
}
