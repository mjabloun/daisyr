% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dai_scaffold.R
\name{scaffold_dai_yaml}
\alias{scaffold_dai_yaml}
\title{Scaffold a starter YAML config for \code{yaml_to_dai()}}
\usage{
scaffold_dai_yaml(sections = names(.DAI_SECTIONS), output_path = NULL)
}
\arguments{
\item{sections}{Character vector of section names from
\code{list_dai_sections()$section}. Defaults to every section.}

\item{output_path}{Character scalar, or \code{NULL} (default). If given, the
generated YAML is also written to this path (via \code{writeLines()}).}
}
\value{
The generated YAML text, as a character scalar, invisibly.
}
\description{
Generates a YAML file containing just the requested top-level sections
(see \code{list_dai_sections()}), always in the same order \code{yaml_to_dai()}
itself emits them in, regardless of the order given here. Placeholder
values are cross-referenced across sections, so the scaffold runs
through \code{yaml_to_dai()} unmodified and produces valid (placeholder-
valued) \code{.dai} output -- replace the placeholders with your own values,
or delete any section you don't need.
}
\examples{
\dontrun{
scaffold_dai_yaml(output_path = "new-config.yaml")
scaffold_dai_yaml(sections = c("activities", "programs", "run"))
}
}
